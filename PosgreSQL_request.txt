create table Person(
    id int generated by default as identity primary key,
    name varchar(100) not null,
    email varchar(100) unique,
    age int check ( age>0 )
);
alter table Person add column email varchar(100) unique;
select * from person;

-- Добавление в таблицу Person двух колонок с различными типами времени
-- Просто дата, пример 03/04/2000.
alter table person add column date_of_birth date;
-- Точное время
-- Значение timestamp сохраняется в секундахдо и после полуночи 01 января 2000 года.
alter table  person add column created_at timestamp;


-- Enum
-- int потому что Ordinal
alter table person add column mood int;
alter table person drop column mood;
-- varchar потому что string
alter table person add column mood varchar;